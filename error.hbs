{{!< default}}

<section class="archive-hero">
  <p class="section-kicker">ERROR</p>
  <h1 class="archive-title">{{statusCode}}</h1>
  <p class="archive-desc">{{message}}</p>
  <a class="btn btn-primary" href="{{@site.url}}">Back Home</a>
</section>
